export enum ScreenMode {
  View = 1,
  Edit = 2,
}

export enum FontSize {
  Regular = 0,
  Small = 1,
  Increased = 2,
  Large = 3,
  Huge = 4,
}

export enum FontType {
  Primary = 0,
  Secondary = 1,
  Error = 2,
  Success = 3,
  Header = 4,
}

export enum InputType {
  Default = 0,
  Numeric = 1,
}
